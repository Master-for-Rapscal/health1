<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.health.mapper.zzh.UserinfoMapper">
    <!-- 分页获取用户列表 -->
    <select id="findList" parameterType="Map" resultType="Userinfo">
        SELECT * from userinfo where 1 = 1
        <if test="username != null">
            and username like '%${username}%'
        </if>
        <if test="roleId != null">
            and roleId = #{roleId}
        </if>
        <if test="sex != null">
            and sex = #{sex}
        </if>
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <!-- 获取符合结果的总记录数 -->
    <select id="getTotal" parameterType="Map" resultType="Integer">
        select count(*) from userinfo where 1 = 1
        <if test="username != null">
            and username like '%${username}%'
        </if>
        <if test="roleId != null">
            and roleId = #{roleId}
        </if>
        <if test="sex != null">
            and sex = #{sex}
        </if>
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>
    <!-- 删除用户 -->
    <delete id="delete" parameterType="String">
        delete from userinfo where userId in(${value})
    </delete>

    <!-- 添加用户 -->
    <insert id="add" parameterType="Userinfo">
INSERT into userinfo(recordName,recordUnit,recordAdress,recordPostalcode,recordPlaceadress,recordCreateuserid,recordResponuserid,recordData,recordMedical,recordResident,
recordBookbuilding,recordCategory,recordInputtingcard) VALUES(#{recordName},#{recordUnit},#{recordAdress},#{recordPostalcode},#{recordPlaceadress},
#{recordCreateuserid},#{recordResponuserid},#{recordData},#{recordMedical},#{recordResident},
#{recordBookbuilding},#{recordCategory},#{recordInputtingcard})
    </insert>
    <!-- 修改用户 -->
    <update id="edit" parameterType="Userinfo">
--         update userinfo set username = #{username},roleId = #{roleId},photo = #{photo},sex = #{sex},age = #{age},address = #{address} where id = #{id}
    </update>

    <select id="findById" parameterType="Long" resultType="Userinfo">
        SELECT * from userinfo where  userId=#{id}

    </select>
</mapper>