<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.health.mapper.womandis.RecordMapper">
    <delete id="delete" parameterType="String">
        delete from womanDisease where wodisId in(${value})
    </delete>


    <!-- 分页获取用户列表 -->
    <select id="findList" parameterType="Map" resultType="com.aaa.health.entity.womandis.Record">
        select w.wodisId,w.userId,u.recordName,u.userIdnumber,u.userBirthday,u.recordAdress,u.userMyphone,u.recordData,w.wodisBh,w.wodisMenage,w.wodisMencycle,w.wodisLoginregtime from womanDisease w left join userinfo u on w.userId=u.userId
        where 1 = 1
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <select id="getTotal" parameterType="Map" resultType="Integer">
        select count(*) from womanDisease w left join userinfo u on w.userId=u.userId where 1 = 1
    </select>

</mapper>