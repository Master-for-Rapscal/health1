<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.health.mapper.busman.AnteBusmanMapper">
    <select id="findList" parameterType="Map" resultType="com.aaa.health.entity.anfoll.AnteFollow">
        select a.anfoId,a.userId,u.recordName,FLOOR((TO_DAYS(SYSDATE())-TO_DAYS(u.userBirthday))/365) userAge,a.anfoPretime,a.anfoLastmenage,c.chbirTime,a.anfoGestational,
        a.anfoParity,a.anfoName,u.recordAdress,u.userMyphone,u.recordData,a.anfoState,a.anfoStatetime,a.anfoStateReason
        from antenatalFollowup a join userinfo u on a.userId=u.userId join childbirth c on a.chbirId=c.chbirId
        where 1=1
        and u.userSex=2
        <if test="username != null">
            and username like '%${username}%'
        </if>
        <if test="roleId != null">
            and roleId = #{roleId}
        </if>
        <if test="sex != null">
            and sex = #{sex}
        </if>
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <update id="update" parameterType="com.aaa.health.entity.anfoll.AnteFollow">
update antenatalFollowup set anfoState=#{anfoState},anfoStatetime=#{anfoStatetime},anfoStateReason=#{anfoStateReason},
anfoColumn=#{anfoColumn} where anfoId=#{anfoId}
</update>

    <!-- 获取符合结果的总记录数 -->
    <select id="getTotal" parameterType="Map" resultType="Integer">
        select count(*) from antenatalFollowup a join userinfo u on a.userId=u.userId join childbirth c on a.chbirId=c.chbirId where 1 = 1
        <if test="username != null">
            and username like '%${username}%'
        </if>
        <if test="roleId != null">
            and roleId = #{roleId}
        </if>
        <if test="sex != null">
            and sex = #{sex}
        </if>
    </select>
</mapper>