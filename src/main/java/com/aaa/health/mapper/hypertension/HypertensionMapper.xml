<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.health.mapper.hypertension.HypertensionMapper">
    <select id="queryAll" parameterType="Map" resultType="Map">

        select * from userinfo join hypertension on
        userinfo.userId=hypertension.recordId join userPrivy on userPrivy.userId=userinfo.userId
        where 1 = 1
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <select id="queryTotal" parameterType="Map" resultType="int">
        select count(*) from userinfo join hypertension on
        userinfo.userId=hypertension.recordId join userPrivy on userPrivy.userId=userinfo.userId   where 1 = 1
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <delete id="deleteById" parameterType="String">
        delete from HYPERTENSION where HYPERTENSION_ID in(${value})
    </delete>

    <select id="queryAllUser" parameterType="Map" resultType="Map">
        select  u.userId,u.recordName,u.userBirthday,u.recordAdress,u.userMyphone from userinfo u,hypertension where u.userId!=hypertension.recordId
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

    <select id="queryUserTotal" parameterType="Map" resultType="Integer">
        select  count(*) from userinfo u,hypertension where u.userId!=hypertension.recordId
        <if test="offset != null and pageSize != null">
            limit #{offset},#{pageSize}
        </if>
    </select>

</mapper>